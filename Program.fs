module Program
open BVTProver
open MathHelpers    
[<EntryPoint>]
let main argv =
    (* Example:
        Your formula: Exists(x, And(Exists(y, Or(x==y, x==a)), And(x==a, x==b)))
        Free formula: a==b
    *)
    0
    


